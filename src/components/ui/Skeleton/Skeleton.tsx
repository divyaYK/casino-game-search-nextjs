import styles from './Skeleton.module.scss';

/**
 * @description Single Game Card style loading component
 */
const Skeleton = () => {
  return <div className={styles.skeleton__box} />;
};

export default Skeleton;
